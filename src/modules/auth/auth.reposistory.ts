import { db } from "../../config/db.js";
export class AuthReposistory{
    async getUserByEmail(email : string) {
        try {
            // todo
        } catch (error) {
            
        }
    }
}